# Миссионеры — родмапа

Что делать дальше и что решено не делать. Появилась из разбора точек роста 13.09.2026 (спека → список → триаж); держать актуальной: сделанное — в «сделано» с датой, новое — в очередь с пометкой, откуда взялось. Технические «куда расти» — `tech.md` §10, очередь по спрайтам — `art.md` §8, дизайнерские открытые вопросы — `design.md` §13.

## Сделано

- **Кадр и среда** (13.09.2026, был п. 1 «кадр бесполезен»): спрайтовые объекты вместо кубов (5 ракурсов, HD-листы, атлас), рельеф по §11б дизайна (холмы, дюны, гряда, расщелина вместо тоннеля, посадочное поле), декорации, тени, туман, скрытность, честный сенсор (усреднение, шум); платформа-тихоходка, миссионеры v5, существо. Инструменты: `tools/render-check.js`, `tools/banner.js`, рецепты в `art.md`.
- **Лидар** (13.09.2026, выросло из п. 1): наклон лучей, барометр 0,1 м, накопленная карта высот с изогипсами, стены линиями; корпус станции — один эллипс для лидара, столкновений, кадра и карты. `tools/hmap-check.js`.
- **Лидар, второй заход** (13.09.2026, по разбору изогипс поперёк стен): бит «сплошное» — измерение парой лучей на теле (хорда круче 40°), а не подсказка мира; датчик на 1,7 м; стены не идут в карту высот и режут сглаживание и изогипсы; грунт в панели отличим от стены; декорации отражают лидар. Лаборатория `index.html?lab` — та же консоль с каналом без ограничений и телепортом. Стены на карте — растр 0,5 м с числом попаданий вместо хранения снимков (19.09.2026): один снимок стены — тусклая линия, много — яркая, без пучков; выключатель «стены».
- **Карта** (13.09.2026): ориентир и предмет разными знаками, подписи без наложений, место объекта с ближайшей съёмки, срок показа пути, выключатели высот и изогипс.
- **Граница мира** (13.09.2026, был п. 3 очереди): станция отказывает по протоколу ПС-2 — цель дальше 500 м от ближайшего узла не принимается, событие 28 с расстоянием; узлы — станция и мачта с включённым усилителем (решено: радиус считается от ближайшего узла, ретранслятор раздвигает границу). Радиус приходит в паспорте, на карте — пунктирные круги вокруг узлов. Координаты в пакетах за пределом обрезаются, не заворачиваются. Бюджет линии к границе не привязан.
- **Редактор уровня** (13.09.2026, был п. 2 очереди). Шаг 1: уровень вынесен в `proto/level.js` (ориентиры и подобъекты с курсом, состоянием и содержимым, колена и ширины расщелины, корпуса, лёжка и логово, точка старта, сюжетные декорации); `world.js` и `terrain.js` читают оттуда, правила видимости по расщелине — из геометрии. Шаг 2: `proto/editor.html` — только localhost; вид сверху с картой высот, изогипсами и непроходимыми склонами, перетаскивание объектов/колен/лёжки/корпусов, панель свойств, линейка и SNR в точке (`Link`), свободная камера через `CAM.renderRaw` в воркере с полётом стрелками, отмена, сохранение POST в `serve.py` или текстом. В шторке игры — телепорт перетаскиванием, правда о мире приходит из мира сообщением. `tech.md` §11.
- **Станция как отдельный хост и сетевой сервер** (18.09.2026, ветка `mp`; решение «мультиплеер — не в этом проекте» отменено: захотелось погонять кооператив раньше). Мир + канал вынесены в `proto/station.js` с чисто сообщенческим интерфейсом; хосты — воркер (одиночная игра, как раньше, без сервера) и Node (`server/index.js`: комнаты, один канал 512 бит/с на всех операторов комнаты, WebSocket, JSON-сохранение на комнату, досылка пропущенных пакетов при повторном входе). Консоль больше не держит канал — вкладка «Канал», лампа и ETA идут по показаниям модема раз в секунду. `tech.md` §1, §9.
- **Отмена запроса** (18.09.2026, из плейтеста вдвоём: кадр 64×64 занимает линию на 80 с, и остановить было нельзя). Команда 27 — буфер станции выбрасывает ещё не переданные пакеты сообщения; до мира не доходит, ушедшие байты не возвращаются, база дельты сбрасывается. Кнопка «✕» — только в очереди на вкладке «Канал» (из панели изображения убрана: вкладка «Канал» получает смысл, интерфейс менее казуальный). `tech.md` §4.
- **Одичалые: стая, рефлексы, крики** (19.09.2026, п. 0б (а) и рефлекторная часть (б)): существо стало стаей из пяти особей с чертами размер / храбрость / внимательность (`level.js` → `pack`, редактор: добавить, перетащить, свойства), чувства через рельеф той же функцией, что лидар, рефлексы по действующей храбрости, крики как физика с радиусом, глушением и кулдауном, эхо тревоги по стае, поводок из удалённости от логова, столкновения как у тел и ходьба по оси расщелины; логово — конец расщелины (прежнее было за стеной, куда существо проходило сквозь скалу). Для оператора: несколько существ в описании (id 250…259), размер и действие в осмотре, страх от криков (звук без слов — до п. 4). `tech.md` §6 «Одичалые», `design.md` §12а (три черты, вход агента = слух вожака). Прогон без браузера — `node tools/pack-check.js [режим] [отбиваться]` (первый сценарий п. 2).
- **Агент стаи: ядро API** (19.09.2026, п. 0б (в), шаги 1–2 + площадка): лента восприятия по особям текстом на смену класса (чужой, своё состояние, место, ориентиры, предметы рядом, ноша, крики, удары), намерения строками «О2: к обломкам» с ответом принято / отказано с причиной, слово будит и держит (`stay`), рефлексы главнее, кулдаун 15 с; предметы у стаи (возьми / положи, ноша замедляет), ящик с брикетом у обломков. Ось «сколько особей слышит агента» — `VOICE_R`, реализовано ∞ («единый мозг», `design.md` §12а пересмотрен). Площадка `tools/agent-play.js`: стая доведена до обломков, взяла брикет и принесла в логово — 33 мин игрового. `tech.md` §6 «Агент стаи». **Турель и команды** (тот же вечер): турель с лампой на ARK-041 — сектор, дальность, захват по движению, прицел 3 с, насмерть, перезарядка; команды платформ `teams` — параметр комнаты, одичалые сами за себя; мёртвые особи лежат; рефлекс на луч — пугливая бежит от света; события 36–37. `tech.md` §6 «Турель платформы». Проверено на площадке: у края дальности бегство спасает, в глубине сектора — нет. **Спектатор** (тот же вечер, из «надо посмотреть твою игру постфактум»): `proto/spectate.html` проигрывает лог мира — карта (слой высот общий с редактором, `mapdraw.js`), интерполяция положений, крики, сектора и захваты турелей, лента стаи / намерений / операторов / заметок, характеристики при наведении; `phys` в логе стал полной правдой о мире; площадка пишет лог через `makeStation`; `serve.py` отдаёт `data/*.log` спектатору.
- **Стойка при контакте, скрытность галочкой, эхо сокомандника** (19.09.2026, п. 0в): режим — только команда (исследование / отступление / отдых); скрытность и стойка «пассивно / бегство / бой» — настройки на теле (команды 24, 25), рефлекс срабатывает по глазам тела без канала (бой — на угрозу в 8 м, бегство — на особь в 15 м, по оси расщелины к узлу) и снимает скрытность на время; всё в байте 15 телеметрии, кнопки — контур «запрошено» / залито «подтверждено»; события 29–34. Сервер отдаёт сокомандникам эхо аплинка до канала — журнал показывает, кто что запросил. Попутно: затухание в расщелине стало плавным у стены (тело, скользящее вдоль стены, теряло и ловило несущую каждый такт). `tech.md` §4, §5, §6, §8; `design.md` §13. SAVE_VERSION 9.
- **Лог мира** (19.09.2026): `station.js` отдаёт хосту каждую запись — команды, доставленные пакеты с байтами, потери, ускорение, раз в секунду правда о положениях, и **заметки мира** `note()` — решения с числами (особь заметила / сменила действие / укусила, крик и кто слышал, отказ ПС-2, смерть с причиной, несущая, застревание, разобранная команда). Сервер пишет `data/КОД.log`, воркер — IndexedDB с кнопкой «лог мира» в шторке; `tools/log-read.js` печатает ленту и сводку. Заметки до операторов не доходят. `tech.md` §12. Сделано 19.09 вечером: спектатор правды о мире (`spectate.html`, см. «Агент стаи»). Не сделано: спектатор глазами оператора (воспроизведение `pkt`/`phys` в консоли с ползунком, перемотка через снимок), отдельный процесс на комнату.
- **Headless-проверки** (п. 9, начато): камера, карта высот, ходьба — всё гоняется в node через шимы воркера (см. `CLAUDE.md` → Проверка). Мир и канал целиком (планировщик, 5 тел, глубина расщелины) — ещё нет.

## Очередь

Порядок — по тому, что ближе к плейтесту.

0. **Мультиплеер** (18.09.2026), по шагам: (а) деплой ветки `mp` на Azure App Service F1 — сервер раздаёт и клиента, GitHub Pages с `main` не трогается; (б) лобби — сделано 18.09 (`lobby.html`: имя оператора, список станций, вход по коду; операторы в шапке консоли); осталось: синхронизация переключателей шапки между консолями (чью подписку включил другой — HB несёт биты кадра, остальное дописать в пульс станции); решено 18.09 и сделано в тот же день: **у каждого оператора всё своё** — комната = один мир на двоих с двумя посадочными платформами (`LEVEL.stations`, ARK-041 и ARK-042), у каждой свои миссионеры, запасы, задача и канал 512 бит/с; общее — рельеф, ориентиры, предметы, существо, мачта. Тела соседа видны в описании и кадре, его мёртвое тело — контейнер. Кооператив остался частным случаем: два оператора на одной платформе. `tech.md` §1 «Платформы»; (в) кооп-плейтест: несколько операторов на одной станции, полосу делят, роли возникают сами; (г) правила и цель на двоих: общий конечный ресурс (камера в расщелине одна, резак один, брикеты), кто первым закрывает ПС-7, помехи как физика (чужой передатчик рядом поднимает шум), встреча тел; асимметрия (орбитальный ретранслятор `orbit` в `link.js`, роботы) — позже. Платформ в уровне четыре (19.09): ARK-042 (180, −150), ARK-043 (60, 220), ARK-044 (250, −60) — все достают до расщелины без ретранслятора (276–360 м), мачта — в середине между всеми, общий спорный объект; число платформ в комнате 2…4 — настройка при создании. Положения выбраны по рельефу наспех — поправить в редакторе после плейтеста. Одиночная игра через воркер остаётся рабочей всегда.
0а. **Связь между платформами через планету** (19.09.2026, из вопроса «а чат?»). Не чат терминалов вне мира (для этого есть Telegram, а игре он ничего не даёт и убивает открытие встречи) — сообщение соседу идёт по-настоящему: вверх на свою станцию → по радио на чужую станцию, если она в зоне слышимости, или через тело рядом с ней → по чужому СВС-каналу вниз. Текст стоит байтов у получателя, стоит в его очереди, не доходит, если станции далеко, передаётся через мачту с усилителем — мачта становится ещё ценнее. Правило из физики, не из кнопки (`codex.md` §4). Нужно: вид пакета `MSG` вниз, команда вверх с текстом, маршрутизация в мире по слышимости (те же SNR, что у тел), размер в `tech.md` §4–5. Делать, когда плейтест покажет, что игроки хотят договариваться, а не просто встречаться.
0б. **Одичалые как третья сторона** (19.09.2026, `design.md` §12а). Шаги по порядку, каждый играбелен: (а) черты и состояния особей, рефлексы в мире — **сделано 19.09** (см. «сделано»); (б) крик как физика: словарь, радиус и глушение, кулдаун, рефлекторные крики и реакции по характеру — **сделано 19.09**; осталось: звук без слов для операторов (после п. 4), крик по команде (с (в)), «добыча» без предметов пока не звучит; (в) агент стаи — **ядро сделано 19.09** (см. «сделано»: восприятие, намерения, предметы, площадка; ось `VOICE_R` при ∞); 3) хост — **HTTP на сервере сделан 19.09** (`packApi`: `join` / `perceive?since&wait` / `act`, токен стаи в настройках комнаты, буфер 2000 строк, лимит 200 мс; документ для агента — `docs/agent-api.md`); **сторона оператора для агента — сделано 19.09** (`/op/…`, виртуальная консоль `server/opconsole.js`: те же пакеты и байты, что у браузерной консоли, текстом; `agent-api.md` §10); в воркере сообщениями — нет (одиночная игра без агента); 4) конечный радиус — вожак (самая храбрая из живых), крики в ленте как слышит вожак (румб, далеко/близко, голос по размеру), «свои рядом»; 5) площадка с турелью — **сделано 19.09**, доведено вечером: турель — отдельный объект у каждой платформы, патроны (магазин 8, ёмкость 24, пачки в ящиках), зона питания 40 м, вкл/выкл со станции (команда 28) и телом, резка чужой резаком (с патронами турель успевает первой), спрайт, секция «турели» и сектор на карте, редактор; команды — каждая платформа сама за себя, одичалые чужие всем. Осталось: раскладка команд в лобби; **ремкомплект** — починка повреждённой турели телом; перенос турелей, розетки и кабели вместо зоны питания; как рисовать луч прожектора в кадре; лампа как свет для чувств стаи ночью; сценарий «выйти из норы, найти обломки, взять предмет, принести в логово, обойти станции» ходится с клавиатуры — следующий шаг: агент (Claude из сессии) вместо клавиатуры, потом HTTP (3). Исходный план (19.09, день): агент сидит в вожаке, вход — то, что слышит вожак: HTTP-API агента с курсором и долгим опросом, честный текст восприятия от особи (без координат, номеров, карты; отладочный флаг на него не действует), намерения строками, токен стаи при создании планеты, мягкий лимит частоты. План реализации (19.09, вечер): 1) `perceive(вожак)` в `world.js` — что видит сама (двуногий со светом / без, близко / далеко, стоит / идёт; тело лежит; предмет), что слышит (крики: слово, направление в 8 румбов, далеко / близко, голос по размеру — «мелкий», «крупный», не номер), своё состояние словами, кто из своих рядом — по размеру и повадке; отдаётся хосту сообщением `{t:'agent'}`, консоли не видят; заготовка — заметки `note()`, они уже честные чувства особи, осталось убрать номера и переписать от лица вожака; 2) намерения `intent(кому, глагол, аргумент)`: «иди к крикнувшему / к чужому / домой / ко мне», «затаись», «крикни слово», «держись рядом», позже «возьми / отнеси» — ложатся в ту же `p.target` / `act:'goto'`, через рефлексы (пугливая может не пойти); адресат — только особь в радиусе слуха вожака (та же `losFrac`, что у крика); кулдаун 10–20 с на особь; ответ принято / отказано с причиной; 3) хост: HTTP на сервере (`join` / `perceive?since&wait` / `act`), токен из настроек комнаты; в воркере — тот же интерфейс сообщениями, чтобы терминал наблюдателя работал локально; 4) вожак — самая храбрая из живых, погибла — следующая, агент узнаёт («голос теперь у крупного»); 5) сначала 1–2 и прогон скриптом вроде `pack-check` — текст восприятия читать глазами до всякого HTTP. Решено: текст — источник истины (не JSON: одна честность на всех, JSON соблазняет утечкой координат); комната с агентом — ×1…×2; без операторов `perceive` висит до таймаута пустым; (г) MCP-адаптер в том же процессе — три инструмента и ресурсы-инструкции, онбординг чужого агента в одну строку; (д) терминал наблюдателя — тот же текст восприятия и ввод намерений для человека, честно «отладка»; (е) психика на нуле → тело уходит к стае (с п. 5 «Паника»); (ж) одичалые снимают камеры с тел, делают знаки. Первая проверка: одно существо в локальной комнате, Claude из сессии раз в 5–60 с читает восприятие и отвечает; смотреть, страшно ли оператору и интересно ли агенту. Сессия RTS: без операторов стая спит.
0г. **Языковой слой агента** (19.09.2026, из вопроса «всё по-русски — как разносить строки»). Сейчас в логе и API: ключи состояний латиницей (`act`, коды событий — как в кодовой книге), лента восприятия — готовый русский текст, намерение — сырая русская строка. Физика от языка уже отделена внутри `perceive` (классы: близко/далеко по метрам, румб по углу, идёт/бежит по скорости) и `intent` (таблица глаголов), но лежит в `world.js` и наружу уходит только текст. Сделать, когда понадобится второй язык или машинный разбор: вынести фразник и таблицу глаголов в `agent-lang.js` (по образцу `codebook.js`); в лог и API писать структурную форму рядом с текстом (`{cls:'see', d:'near', rumb:2, light:1, mv:'walk'}` + `text`); намерения принимать и по каноническим глаголам. Текст остаётся источником честности (никаких координат ни в одной из форм). Не откладывать до момента, когда текста станет много.
0в. **Стойка при контакте вместо кнопок режима** — **сделано 19.09.2026** (см. «сделано»; осталось: звук на события 29–34 — в п. 4). Было: (из плейтеста стаи: сигнал был, действия не было — оператор не знал, что нажать, а канал для рефлекса слишком медленный). Инструкция на потерю связи — прецедент: поведение задано заранее, срабатывает по триггеру без канала. Контакт с одичалым — такой же триггер. Симметрия с §12а: у особи храбрость — черта, у миссионера — стойка, выставленная оператором. Что сделать: (а) **скрытность — галочка**, ортогональна всему (свет, скорость, шум шагов); (б) **при контакте: бегство / пассивно / бой** — тело реагирует само, когда *само видит* особь вблизи или получает удар (по своим глазам, не по биту правды): бегство — бежит к ближайшему узлу, пока чужих не видно 20 с, докладывает событием; бой — бьёт, пока чужой в досягаемости, потом возвращается к делу; пассивно — как сейчас; (в) отдых и отступление остаются командами, отступление — и инструкцией на потерю связи. Один байт стойки, один раз; `tech.md` §4. Меняет §13 «прямое управление» → «цели — прямо, рефлексы тела — стойкой»: `design.md` §13 поправить при реализации.
0г. **Боевая система и турель** (19.09.2026, после п. 0б — поведения стаи и агентского управления). Турель — один из немногих честных способов боя миссионеров; предмет, который носят, ставят и питают. Патроны против лазера с питанием — открыто (`design.md` §13). За ней — биомасса как ресурс (`design.md` §12б): переработка тел, эвакуация, борьба за мёртвого.
1. **Вторые состояния в кадре** (из п. 1): вскрытый штабель, раскопанная насыпь, шкаф с горящим индикатором, существо «бьёт». Без этого столп «картинка — плати за правду» работает только на существе и телах. Листы — `art.md` §8, выбор листа по `objState` — `camera.js`. Туда же **камера мёртвого тела лежит** (19.09.2026): высота глаз 0,3 м вместо 1,6, крен ~80°, курс — куда упало; `renderRaw` умеет высоту и курс, крена нет — рендерить чуть шире и поворачивать кадр. Кадр с мёртвого тела и так снимается (приборы на остатке заряда) — станет видно, что оно мёртвое, без единого байта.
2а. **Спектатор** (19.09.2026, из лога мира): консоль в режиме воспроизведения — транспорт читает JSONL вместо сокета, `pkt` идут как живые, рядом правда из `phys`, ползунок времени; перемотка назад — через снимок + догон (`catchUp`). Тот же `console.js`, без второго рендера. После — разбор смыслом по ленте `log-read` агентом.
2. **Headless-прогон мира и канала** (п. 9): решить, что тестировать — сценарии вроде «5 тел, секундная телеметрия → пульс станции проходит», «дно расщелины без усилителя → 0 пакетов», «команда в очереди при занятой линии». Подход: один сценарий = один файл в `tools/`, печатает числа, сравнивать глазами; без фреймворка.
3. **Срежиссированные узкие места канала** (из п. 3): базово канал не расширять — сейчас команды проходят, узость чувствуется. Нужны конкретные моменты, где придётся выключать всё ради одного потока (глубина расщелины, два тела в поле, кадр 64×64 при слабой несущей). Придумываются вместе с содержанием второго акта.
4. **Звук как подписка** (13.09.2026). Мир звук не рендерит: акустическая сцена — источники с положением и спектром (ветер по открытости места: равнина / за гребнем / расщелина; капель и геотермика на дне; дыхание и шаги существа; своё сердце и дыхание из `exertion` и `fear`; гул станции), мир считает энергию по 4–8 полосам в точке миссионера с затуханием и перекрытием стенами — теми же функциями, что лидар. По каналу — огибающая: 8 полос × 4 бита при 4 кадрах/с = 16 Б/с, или 4 полосы при 2 кадрах/с = 4 Б/с; новый `kind`, класс `bg`, размер в `tech.md` §4–5, выключатель и интервал в шапке, Б/с в легенде «Канала». Консоль синтезирует локально (Web Audio: шум через полосовые фильтры с огибающими) — как ЭКГ по байту пульса. Держит столп «тело реагирует раньше»: низкая полоса растёт, когда существо в 6 м, раньше описания. Честные следствия, не режиссура: устаревшая огибающая затухает как приглушённая телеметрия, потерянный пакет — дроп, страх поднимает сердце и дыхание тела и маскирует внешнее. Первый шаг — локальные звуки за ноль байт, и в первую очередь **уведомления на события** (19.09.2026, из плейтеста: 65 с горел бит «опасность» и десять «повреждений» — оператор ничего не заметил): смена бита «опасность», каждое «повреждения», потеря и возврат несущей, смерть, прибытие — консоль озвучивает то, что уже приняла; потом писк ЭКГ по пульсу, несущая как тон с шипением по SNR из пульса станции, реле команд. Полный речевой поток (LPC ≈ 700 бит/с, весь канал) — не планируется.
5. **Паника** (п. 2, бэклог): психика на нуле — тело замирает или уходит само; позже — препараты. «Нормальная вещь, будет прикольно и цинично», но не сейчас.
5а. **Токсины** (19.09.2026, из плейтеста: 255 на шкале и полоска за панелью): растут в расщелине (0,06/с, шкала до 100), но последствий нет: показатель без физики. Решить вместе с психикой на нуле.
6. **Поиск пути**: может понадобиться (расщелина, обход корпусов); сопротивляться изящно — точки по коленам, цель внутри корпуса переносится наружу (сделано), скольжение. Настоящий поиск — только если плейтест покажет, что игроки застревают.
7. **Кодовая книга как прогрессия** (п. 7 — не был понят, объяснить заново): сейчас существо в описании — «неопознанное» (класс 2, тип 250) всегда. Идея: тип получает имя в книге станции после того, как игрок получил его кадр, — описание начинает писать «существо» вместо «неопознанное». Картинка становится способом *научить* станцию, не только увидеть. Зависит от п. 1.

## Следующие акты (не в этом)

- **Смерть и биоматериал** (п. 5): тел пять, потеря ощущается; биоматериал станет конечным. Конфликт усиливать в следующих главах, в этом акте не трогать.
- **Вычисления как ресурс** (п. 6): сбор информации на стороне миссионера автоматический и бесплатный; калькуляции — в значимых срежиссированных местах, для них нужна обвязка, но сначала ситуации — задача следующих актов.
- **Возвращение живым = слив записанного** (из первого списка, п. 5 там): тело копит полные кадры и описания, отдаёт при контакте со шлюзом. Меняет решение «дальше или назад». Тоже к следующим актам — сейчас смерть и так стоит.
- **Второй акт контента** (п. 8): ретранслятор как награда (вторая зона), второй датчик. Отдельно, не сейчас.

## Решено не делать

- **Расширять канал** — нет, узость заметна и это правильно; только режиссура моментов.
- **Граница мира через физику линии или кольцо гор** (13.09.2026) — нет: дБ оставить для другого, ландшафтная стена заставит ставить объекты «до горы». Граница — отказ станции по протоколу (сделано).
- **Режиссировать тишину и «страшный голос» отдельно** (13.09.2026) — нет: огибающая по полосам, затухание устаревших данных и маскировка страхом дают это сами. Фантомные звуки от психики на нуле — ложь в данных, ломает столп 1.
- **Мета-слой «кто сидит за терминалом»** (п. 4) — выкинут целиком: игрок и есть тот, кто получил ключ. Ничего про отца, наследство, историю оператора.
- **Станция-соперник с ИИ-оператором, фракции с другим типом тел** — не в этом проекте (сервер и кооператив — см. очередь, п. 0; решение 13.09 «мультиплеер — не в этом проекте» пересмотрено 18.09; PvE-сторона — одичалые, п. 0б, 19.09).
- **Чат терминалов вне мира** — нет: связь только через планету (п. 0а).
- **Дельты в логе мира** — нет (19.09.2026): полный кадр `phys` раз в секунду — 1,6 КБ/с сейчас, ~55 МБ/час при ×10 юнитов; это приемлемо, логи не обязаны храниться вечно, а полный кадр проще отлаживать и проигрывать. Резервы, если понадобятся: писать только изменившееся, короткие ключи, реже для спящих — ×5…10.
- **Рой / общий мозг стаи** — нет: агент — вожак и голос, не сознание всех; каждая особь — бывший миссионер со своим характером (п. 0б).
- **Живой мир без операторов** — нет, сессия RTS: стая спит, когда никто не подключён.
- **Кодек кадров** — не нужен, честности размера хватает.
- **3D-рендер сцены** — рассмотрен в п. 1, выбраны спрайты на рельефе; возвращаться не планируется.
- **Мобильный клиент** (п. 10) — только если база игроков вырастет.
